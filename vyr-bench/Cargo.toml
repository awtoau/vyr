[package]
name = "vyr-bench"
description = "F2 perf harness: deterministic ns/px benches, committed baseline, the scaling-law assertion (run|record|check)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
vyr-core = { path = "../vyr-core" }
serde_json = "1"
